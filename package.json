{
    "name": "smart-slidegen-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "client": "cd client && npm start",
        "server": "cd server && uvicorn server:app --reload --host 0.0.0.0 --port 8000",
        "dev": "concurrently \"npm run client\" \"npm run server\""
    },
    "devDependencies": {
        "concurrently": "^7.6.0"
    }
}
